{
  "name": "ilanglab",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:gh": "astro build",
    "cf:project:create": "wrangler pages project create ilanglab",
    "deploy:cf": "npm run build && wrangler pages deploy dist --project-name=ilanglab"
  },
  "dependencies": {
    "astro": "^4.15.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
